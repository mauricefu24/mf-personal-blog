version: "3.9"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:./dev.db
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: changeme
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
